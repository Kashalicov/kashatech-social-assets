# kashatech-social-assets

Repositório de hospedagem de imagens para agendamento automático de posts do
Instagram (@kashatech) via API do Buffer.

As imagens em `posts/` são servidas via `raw.githubusercontent.com` como URLs
públicas para a mutation `createPost` do Buffer, que não possui endpoint de
upload próprio.
